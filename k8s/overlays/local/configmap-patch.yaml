# Local development ConfigMap overrides
# Enables debug logging, permissive CORS, local-friendly settings
apiVersion: v1
kind: ConfigMap
metadata:
  name: fte-config
  namespace: fte-production
data:
  DATABASE_URL: "postgresql://fte:fte_password@postgres-service:5432/fte_crm"
  KAFKA_BROKERS: "kafka-service:9092"
  LOG_LEVEL: "DEBUG"
  CORS_ORIGINS: "*"
  API_HOST: "0.0.0.0"
  API_PORT: "8000"
  OPENAI_MODEL: "gpt-4o"
  OPENAI_EMBEDDING_MODEL: "text-embedding-3-small"
