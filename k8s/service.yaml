apiVersion: v1
kind: Service
metadata:
  name: fte-api-service
  namespace: fte-production
spec:
  selector:
    app: fte-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
